(function (angular) {
    'use strict';

    const arkanoid = angular.module('arkanoid');

    arkanoid.controller('HighscoresController', ['$scope', 'highscores', function ($scope, highscores) {
        /*
         Assignment 7: when the promised data is resolved this controller is injected with the data
         All we need to do is add it to the scope to make it available to the view
         */

    }]);

})(window.angular);